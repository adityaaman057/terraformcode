# Terraformcommaands

terraform init :- Initialize the backend and download the required plugins, modules and create the environment files.

terraform plan :- Preview the changes, terraform perform before applying them.

terraform apply :- Execute the planned changes & provisions the infrastructure.

terraform destroy :- Safely remove all the resources managed by terraform.
